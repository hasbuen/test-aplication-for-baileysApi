/**
 * Exporta o enum `EMensagem` que contém mensagens utilizadas na aplicação.
 * @see {@link EMensagem} Para detalhes sobre as mensagens disponíveis.
 */
export { default as EMensagem } from './MensagensEnum';

/**
 * Exporta o enum `ERotulos` que contém rótulos utilizados na aplicação.
 * @see {@link ERotulos} Para detalhes sobre os rótulos disponíveis.
 */
export { default as ERotulos } from './RotulosEnum';