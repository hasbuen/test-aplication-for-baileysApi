/**
 * Exporta o controlador de menu de configuração.
 *
 * Este módulo fornece uma interface para manipular a lógica do menu de configuração.
 *
 * @module ConfigMenuController
 * @preferred
 * @see {@link ./ConfigMenuUtil} Para mais detalhes sobre a implementação.
 */
export { default as ConfigMenuController } from './ConfigMenuUtil';

/**
 * Exporta o controlador de formulário de arquivo.
 *
 * Este módulo fornece uma interface para manipular a lógica do envio de arquivos através de um formulário.
 *
 * @module FileFormController
 * @preferred
 * @see {@link ./FileFormUtil} Para mais detalhes sobre a implementação.
 */
export { default as FileFormController } from './FileFormUtil';

/**
 * Exporta o controlador de formulário de texto.
 *
 * Este módulo fornece uma interface para manipular a lógica do envio de texto através de um formulário.
 *
 * @module TextFormController
 * @preferred
 * @see {@link ./TextFormUtil} Para mais detalhes sobre a implementação.
 */
export { default as TextFormController } from './TextFormUtil';